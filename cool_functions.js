const os=require('os');
console.log(os.freemem()); // Lists free memory on the system.
console.log(os.homedir()); // Lists the home directory of the system
console.log(os.hostname()); // Lists the system name.    